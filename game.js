class Game {
  construct(width, height) {
    this.width = width;
    this.height = height;
  }
  update() {
    img = document.getElementById(img);
    ctx.drawImage(img, x, y, width, height);
  }
}
